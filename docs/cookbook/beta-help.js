/* Clear starting actions and local beta guidance; no automatic data sharing. */
'use strict';
const betaHome=home;
home=()=>{const wrapper=document.createElement('div');wrapper.innerHTML=betaHome();for(const section of wrapper.querySelectorAll('.all-entry,.library-entry,.tailored-strip'))section.remove();for(const section of wrapper.querySelectorAll('section.panel'))if(section.querySelector('[data-action="southern-browse"],[data-action="easyweb-browse"]'))section.remove();const start=wrapper.querySelector('.shelf-start');if(start)start.insertAdjacentHTML('afterend',`<section class="panel"><h2>Find your next meal</h2><p class="small">Your saved recipes are below. Browse suggestions and source notes separately.</p><div class="row wrap">${button('Browse all recipes & notes','all-browse','primary')}${button('250 Southern recipes','southern-browse')}${button('500 easy web recipes','easyweb-browse')}${button('Explore 500 recipes','library-browse')}${button('10 recipes picked for you','tailored-browse')}${button('Help me pick dinner','tonight-pick')}</div></section>`);return wrapper.innerHTML;};
const betaSettings=settings;
settings=()=>betaSettings()+`<section class="panel"><h2>Privacy & beta testing</h2><p>Recipes, photos and notes are stored on this device. No analytics are installed. Exports contain the data you choose to export. The external creator sends your submitted prompt to its provider when you deliberately generate.</p><p>Before testing an update, export a backup. Keep the existing app installed. This is a preview; recipes and automatic measurements need your cooking review.</p><p>To report a problem, record what you tapped, what happened, what you expected, and your app version. Remove personal information before sharing screenshots. No report is sent automatically.</p></section>`;
render();
